{
  "name": "digitalindo-static",
  "version": "1.0.0",
  "private": true,
  "description": "Digitalindo - static landing page (HTML/CSS/JS only)",
  "scripts": {
    "start": "python3 -m http.server 3000 --bind 0.0.0.0 --directory /app/static-site"
  }
}
